# FoodFresh AI — production dependencies
# Target: Python 3.13 / Linux (Render). Versions pinned to 3.13-safe floors.

# Web framework
Flask>=3.0,<4
flask-login>=0.6
flask-sqlalchemy>=3.1
flask-limiter>=3.5
Werkzeug>=3.0
SQLAlchemy>=2.0
gunicorn>=21.2

# Computer vision / ML inference
# PyTorch wheels for Linux (Render) are installed FIRST in build.sh from the
# CPU-only index (https://download.pytorch.org/whl/cpu) to keep the image lean.
# torch/torchvision floors below are the earliest releases with Python 3.13.
torch>=2.6.0
torchvision>=0.21.0
opencv-python-headless>=4.10
Pillow>=10.0
numpy>=1.26,<3

# Reports
reportlab>=4.0
qrcode>=7.4

# PostgreSQL driver (used automatically when DATABASE_URL is set)
psycopg2-binary>=2.9